#!/usr/bin/env ruby

File.open(ARGV[0], 'w+') do |file|
	file.puts "config_vars:"
	file.puts "  FOO: bar"
	file.puts "default_process_types:"
	file.puts "  web: mockingjay-server -config=server.yaml -port=$PORT"
end


